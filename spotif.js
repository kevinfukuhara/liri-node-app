// Set up variables for spotify.
var keys = require("./keys.js");
var spotify = new Spotify(keys.spotify);

// require the correct npm packages
var Spotify = require("node-spotify-api");
var axios = require("axios");

